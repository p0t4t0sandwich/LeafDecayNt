pluginManagement {
    repositories {
        gradlePluginPortal()

        // Fabric
        maven {
            name = 'Fabric'
            url = 'https://maven.fabricmc.net/'
        }
    }
}

rootProject.name = 'LeafDecayNt'

include(
        'bukkit',
        'fabric',
        'forge',
        'common'
)
